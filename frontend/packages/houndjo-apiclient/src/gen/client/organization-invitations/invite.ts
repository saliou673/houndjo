/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */
import fetch from "@kubb/plugin-client/clients/axios";
import type {
    Client,
    RequestConfig,
    ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
    InviteMutationRequest,
    InviteMutationResponse,
    InvitePathParams,
    InviteHeaderParams,
    Invite403,
    Invite404,
    Invite409,
    Invite500,
} from "../../types/Invite.ts";

function getInviteUrl(orgId: InvitePathParams["orgId"]) {
    const res = {
        method: "POST",
        url: `/api/organizations/${orgId}/invitations` as const,
    };
    return res;
}

/**
 * {@link /api/organizations/:orgId/invitations}
 */
export async function invite(
    orgId: InvitePathParams["orgId"],
    data: InviteMutationRequest,
    headers?: InviteHeaderParams,
    config: Partial<RequestConfig<InviteMutationRequest>> & {
        client?: Client;
    } = {}
) {
    const { client: request = fetch, ...requestConfig } = config;

    const requestData = data;

    const res = await request<
        InviteMutationResponse,
        ResponseErrorConfig<Invite403 | Invite404 | Invite409 | Invite500>,
        InviteMutationRequest
    >({
        method: "POST",
        url: getInviteUrl(orgId).url.toString(),
        data: requestData,
        ...requestConfig,
        headers: { ...headers, ...requestConfig.headers },
    });
    return res.data;
}
