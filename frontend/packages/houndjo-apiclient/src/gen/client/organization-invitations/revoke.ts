/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */
import fetch from "@kubb/plugin-client/clients/axios";
import type {
    Client,
    RequestConfig,
    ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
    RevokeMutationResponse,
    RevokePathParams,
    RevokeHeaderParams,
    Revoke403,
    Revoke404,
    Revoke409,
    Revoke500,
} from "../../types/Revoke.ts";

function getRevokeUrl(
    orgId: RevokePathParams["orgId"],
    id: RevokePathParams["id"]
) {
    const res = {
        method: "DELETE",
        url: `/api/organizations/${orgId}/invitations/${id}` as const,
    };
    return res;
}

/**
 * {@link /api/organizations/:orgId/invitations/:id}
 */
export async function revoke(
    orgId: RevokePathParams["orgId"],
    id: RevokePathParams["id"],
    headers?: RevokeHeaderParams,
    config: Partial<RequestConfig> & { client?: Client } = {}
) {
    const { client: request = fetch, ...requestConfig } = config;

    const res = await request<
        RevokeMutationResponse,
        ResponseErrorConfig<Revoke403 | Revoke404 | Revoke409 | Revoke500>,
        unknown
    >({
        method: "DELETE",
        url: getRevokeUrl(orgId, id).url.toString(),
        ...requestConfig,
        headers: { ...headers, ...requestConfig.headers },
    });
    return res.data;
}
