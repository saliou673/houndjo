/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */
import fetch from "@kubb/plugin-client/clients/axios";
import type {
    Client,
    RequestConfig,
    ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
    ListQueryResponse,
    ListPathParams,
    ListQueryParams,
    ListHeaderParams,
    List403,
    List404,
    List409,
    List500,
} from "../../types/List.ts";

function getListUrl(orgId: ListPathParams["orgId"]) {
    const res = {
        method: "GET",
        url: `/api/organizations/${orgId}/invitations` as const,
    };
    return res;
}

/**
 * {@link /api/organizations/:orgId/invitations}
 */
export async function list(
    orgId: ListPathParams["orgId"],
    params: ListQueryParams,
    headers?: ListHeaderParams,
    config: Partial<RequestConfig> & { client?: Client } = {}
) {
    const { client: request = fetch, ...requestConfig } = config;

    const res = await request<
        ListQueryResponse,
        ResponseErrorConfig<List403 | List404 | List409 | List500>,
        unknown
    >({
        method: "GET",
        url: getListUrl(orgId).url.toString(),
        params,
        ...requestConfig,
        headers: { ...headers, ...requestConfig.headers },
    });
    return res.data;
}
