/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */
import fetch from "@kubb/plugin-client/clients/axios";
import type {
    Client,
    RequestConfig,
    ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
    EndEnrollmentMutationResponse,
    EndEnrollmentPathParams,
    EndEnrollmentHeaderParams,
    EndEnrollment403,
    EndEnrollment404,
    EndEnrollment409,
    EndEnrollment500,
} from "../../types/EndEnrollment.ts";

function getEndEnrollmentUrl(id: EndEnrollmentPathParams["id"]) {
    const res = {
        method: "POST",
        url: `/api/v1/enrollments/${id}/end` as const,
    };
    return res;
}

/**
 * {@link /api/v1/enrollments/:id/end}
 */
export async function endEnrollment(
    id: EndEnrollmentPathParams["id"],
    headers?: EndEnrollmentHeaderParams,
    config: Partial<RequestConfig> & { client?: Client } = {}
) {
    const { client: request = fetch, ...requestConfig } = config;

    const res = await request<
        EndEnrollmentMutationResponse,
        ResponseErrorConfig<
            | EndEnrollment403
            | EndEnrollment404
            | EndEnrollment409
            | EndEnrollment500
        >,
        unknown
    >({
        method: "POST",
        url: getEndEnrollmentUrl(id).url.toString(),
        ...requestConfig,
        headers: { ...headers, ...requestConfig.headers },
    });
    return res.data;
}
