/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */
import fetch from "@kubb/plugin-client/clients/axios";
import type {
    Client,
    RequestConfig,
    ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
    SetPaceMutationRequest,
    SetPaceMutationResponse,
    SetPacePathParams,
    SetPaceHeaderParams,
    SetPace403,
    SetPace404,
    SetPace409,
    SetPace500,
} from "../../types/SetPace.ts";

function getSetPaceUrl(courseId: SetPacePathParams["courseId"]) {
    const res = {
        method: "PUT",
        url: `/api/v1/courses/${courseId}/pace` as const,
    };
    return res;
}

/**
 * {@link /api/v1/courses/:courseId/pace}
 */
export async function setPace(
    courseId: SetPacePathParams["courseId"],
    data: SetPaceMutationRequest,
    headers?: SetPaceHeaderParams,
    config: Partial<RequestConfig<SetPaceMutationRequest>> & {
        client?: Client;
    } = {}
) {
    const { client: request = fetch, ...requestConfig } = config;

    const requestData = data;

    const res = await request<
        SetPaceMutationResponse,
        ResponseErrorConfig<SetPace403 | SetPace404 | SetPace409 | SetPace500>,
        SetPaceMutationRequest
    >({
        method: "PUT",
        url: getSetPaceUrl(courseId).url.toString(),
        data: requestData,
        ...requestConfig,
        headers: { ...headers, ...requestConfig.headers },
    });
    return res.data;
}
